feature "API Health Check"

actors: Web

scenario "Server is healthy" {
    test HealthCheckReturns200 "The health check endpoint returns a 200 OK status" {
        given:
            wait >= 0s
        when:
            Web http_get "https://httpbin.org/status/200"
        then:
            Web response_status_is 200
    }
}